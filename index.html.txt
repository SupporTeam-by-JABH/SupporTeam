<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SuportTeam</title>

<style>
body {
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    color: white;
    overflow-x: hidden;
    background: linear-gradient(120deg, #6a00f4, #00d4ff, #00ffc8);
    background-size: 300% 300%;
    animation: gradientMove 15s ease infinite;
}

@keyframes gradientMove {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

.container {
    text-align: center;
    padding-top: 120px;
    min-height: 100vh;
}

h1 {
    font-size: 50px;
    font-weight: 800;
}

.slogan {
    font-weight: 600;
    opacity: 0.9;
    margin-bottom: 50px;
}

.card {
    background: rgba(0,0,0,0.45);
    padding: 50px;
    border-radius: 30px;
    display: inline-block;
    backdrop-filter: blur(20px);
    transition: transform 0.3s ease;
}

.card.zoom {
    transform: scale(1.05);
}

input {
    padding: 16px;
    width: 260px;
    border-radius: 15px;
    border: 2px solid rgba(255,255,255,0.4);
    background: rgba(0,0,0,0.5);
    color: white;
    font-size: 17px;
    font-weight: 600;
    margin-right: 10px;
    outline: none;
    transition: all 0.3s ease;
}

input::placeholder {
    color: rgba(255,255,255,0.9);
    font-weight: 700;
}

input:focus {
    border: 2px solid #00ffc8;
    box-shadow: 0 0 15px rgba(0,255,200,0.6);
}

button {
    padding: 15px 25px;
    border-radius: 15px;
    border: none;
    background: linear-gradient(90deg, #6a00f4, #00ffc8);
    color: white;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
}

.result {
    margin-top: 35px;
    font-size: 20px;
    font-weight: 800;
}

.whatsapp-btn {
    position: fixed;
    bottom: 25px;
    right: 25px;
    background: #25D366;
    color: white;
    font-size: 28px;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: none;
    justify-content: center;
    align-items: center;
    text-decoration: none;
    box-shadow: 0 5px 20px rgba(0,0,0,0.4);
}

footer {
    position: fixed;
    bottom: 10px;
    width: 100%;
    text-align: center;
    font-size: 14px;
    opacity: 0.7;
}
</style>
</head>

<body>

<div class="container">
    <h1>SuportTeam</h1>
    <p class="slogan">Un click, múltiples soluciones</p>

    <div class="card">
        <h2>Consulta tu estatus</h2>
        <input type="text" id="folio" placeholder="Ingresa tu folio">
        <button onclick="consultar()">Consultar</button>
        <div class="result" id="resultado"></div>
    </div>
</div>

<a id="whatsappLink" target="_blank" class="whatsapp-btn">💬</a>

<footer>
Creado, editado y animado por CHATGPT y YISUSM98
</footer>

<audio id="clickSound" src="notificacion.mp3"></audio>
<audio id="successVoice" src="listo.mp3"></audio>

<script>
function consultar() {

    document.getElementById("clickSound").play();

    const resultado = document.getElementById("resultado");
    const folio = document.getElementById("folio").value;
    const card = document.querySelector(".card");
    const whatsappBtn = document.getElementById("whatsappLink");

    card.classList.add("zoom");
    setTimeout(() => card.classList.remove("zoom"), 300);

    let estatus = "";
    let color = "";

    if(folio === "ST-6") {
        estatus = "📌 Estatus: Listo para entrega";
        color = "#00ffc8";

        document.getElementById("successVoice").play();

        let mensaje = `Hola SuportTeam, acabo de consultar mi folio ${folio} Aparece como listo en que horario puedo pasar a recogerlo`;
        whatsappBtn.href = "https://wa.me/521XXXXXXXXXX?text=" + encodeURIComponent(mensaje);
        whatsappBtn.style.display = "flex";
    } else {
        estatus = "📌 Estatus en proceso";
        color = "#a020f0";
        whatsappBtn.style.display = "none";
    }

    resultado.innerHTML = "";
    resultado.style.color = color;

    let i = 0;
    function typeWriter() {
        if (i < estatus.length) {
            resultado.innerHTML += estatus.charAt(i);
            i++;
            setTimeout(typeWriter, 40);
        }
    }

    typeWriter();
}
</script>

</body>
</html>